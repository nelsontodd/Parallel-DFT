# Examples of numerical methods parallelized with MPI
## Build: 
`Make all`. Ensure mpicc works on your system and OMP/MPI are installed.
## Clean:
`Make clean`.
## Structure:
src/ contains all code.
bin/ contains all binaries.
